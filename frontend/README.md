# JobFinder Frontend

Vite + React (JS) frontend for the JobFinder backend.

## Setup

```bash
npm install
```

Create `frontend/.env` if backend is not on default:
```
VITE_API_URL=http://localhost:8000
```

## Run

```bash
npm run dev
```

## Build

```bash
npm run build
```
